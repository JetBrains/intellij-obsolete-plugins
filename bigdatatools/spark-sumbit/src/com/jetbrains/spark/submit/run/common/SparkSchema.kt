package com.jetbrains.spark.submit.run.common

object SparkSchema {
  const val MASTER = "--master"
  const val VERBOSE = "--verbose"
  const val SUPERVISE = "--supervise"
  const val CLAZZ = "--class"
  const val DEPLOY_MODE = "--deploy-mode"
  const val NAME = "--name"
  const val QUEUE = "--queue"
  const val PROXY_USER = "--proxy-user"
  const val PROPERTIES_FILE = "--properties-file"
  const val CONF_PARAM = "--conf"
  const val JARS = "--jars"
  const val PY_FILES = "--py-files"
  const val FILES = "--files"
  const val PACKAGES = "--packages"
  const val REPOSITORIES = "--repositories"
  const val EXCLUDE_PACKAGES = "--exclude-packages"
  const val DRIVER_MEMORY = "--driver-memory"
  const val DRIVER_JAVA_OPTS = "--driver-java-options"
  const val DRIVER_LIB_PATH = "--driver-library-path"
  const val DRIVER_CLASS_PATH = "--driver-class-path"
  const val DRIVER_CORES = "--driver-cores"
  const val EXECUTOR_MEMORY = "--executor-memory"
  const val EXECUTOR_CORES = "--executor-cores"
  const val NUM_EXECUTORS = "--num-executors"
  const val TOTAL_EXECUTOR_CORES = "--total-executor-cores"
  const val ARCHIVES = "--archives"
  const val PRINCIPAL = "--principal"
  const val KEYTAB = "--keytab"
}