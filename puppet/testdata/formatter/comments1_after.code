# This is toplevel comment
notice("!")

if (1 + 1 == 2) {
  # }}}
  # {{{ Setup postgres
  class { 'postgresql::globals':
    # this is version
    version             => '9.4',
    manage_package_repo => true,
    encoding            => 'UTF8',
  }
}

node default {
  # core modules, needed for most things
  include git
}

node default {
  include git
  # core modules, needed for most things
}